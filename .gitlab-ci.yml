stages:
  - build

variables:
  DOCKER_IMAGE_NAME: "ece2crt/airline_system"
  DOCKER_IMAGE_TAG: "1.0"

before_script:
  - echo "Install Docker..."
  - apt-get update -qy
  - apt-get install -y docker.io

build-job:
  stage: build
  script:
    - echo "Building Docker image..."
    - docker build -t airline_system:1.0 -f "Airline/Dockerfile" .
    